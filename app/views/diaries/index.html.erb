<div class="container pt-3">
  <div class="row">
    <div class="col-lg-10 offset-lg-1">
      <!-- 検索フォーム -->
      <%= render 'search_form', url: diaries_path %>
    </div>
  </div>

  <!-- ストレス解消法一覧 -->
  <div class="row">
    <div class="col-12">
      <div class="row">
        <%= render @diaries %>
      </div>
      <!-- ページネーション -->
      <%= paginate @diaries %>
    </div>
  </div>
</div>