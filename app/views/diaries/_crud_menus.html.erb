<ul class='crud-menu-btn list-inline float-right'>
  <li class="list-inline-item">
    <%= link_to diary_path(diary), id: "button-delete-#{diary.id}", data: { turbo_method: :delete, turbo_confirm: "削除しますか？" } do %>
      <i class="bi bi-trash"></i>
      Delete
    <% end %>
  </li>
</ul>
